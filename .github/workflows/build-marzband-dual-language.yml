name: Build Marzband From ZIP

on:
  workflow_dispatch:
    inputs:
      zip_name:
        description: "Optional ZIP filename in repository root"
        required: false
        default: ""

  push:
    branches:
      - main
      - master
    paths:
      - "**/marzband_flutter_*.zip"
      - "**/marzband_complete_*.zip"
      - ".github/workflows/build-marzband-dual-language.yml"

permissions:
  contents: read
  actions: write

concurrency:
  group: marzband-dual-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-release:
    name: Build signed release outputs
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter stable
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install required tools
        run: |
          set -Eeuo pipefail

          sudo apt-get update
          sudo apt-get install -y unzip rsync

          flutter --version
          java -version

      - name: Find and extract source ZIP
        id: extract
        env:
          REQUESTED_ZIP: ${{ github.event.inputs.zip_name }}
        run: |
          set -Eeuo pipefail

          mkdir -p \
            "$GITHUB_WORKSPACE/extracted" \
            "$GITHUB_WORKSPACE/dist" \
            "$GITHUB_WORKSPACE/build-logs"

          ZIP_FILE=""

          if [[ -n "${REQUESTED_ZIP:-}" ]]; then
            if [[ ! -f "$GITHUB_WORKSPACE/$REQUESTED_ZIP" ]]; then
              echo "::error::Requested ZIP not found: $REQUESTED_ZIP"
              exit 1
            fi

            ZIP_FILE="$GITHUB_WORKSPACE/$REQUESTED_ZIP"
          fi

          if [[ -z "$ZIP_FILE" ]]; then
            CHANGED_ZIP="$(
              git diff-tree \
                --no-commit-id \
                --name-only \
                -r HEAD \
              | grep -E '(^|/)(marzband_flutter|marzband_complete).*\.zip$' \
              | tail -n 1 \
              || true
            )"

            if [[ -n "$CHANGED_ZIP" && -f "$CHANGED_ZIP" ]]; then
              ZIP_FILE="$GITHUB_WORKSPACE/$CHANGED_ZIP"
            fi
          fi

          if [[ -z "$ZIP_FILE" ]]; then
            ZIP_FILE="$(
              find "$GITHUB_WORKSPACE" \
                -maxdepth 2 \
                -type f \
                \( \
                  -name 'marzband_flutter_*.zip' \
                  -o \
                  -name 'marzband_complete_*.zip' \
                \) \
                -print0 \
              | while IFS= read -r -d '' candidate; do
                  relative="${candidate#${GITHUB_WORKSPACE}/}"

                  committed_at="$(
                    git log \
                      -1 \
                      --format=%ct \
                      -- "$relative" \
                      2>/dev/null \
                    || true
                  )"

                  printf '%s\t%s\n' \
                    "${committed_at:-0}" \
                    "$candidate"
                done \
              | sort -n \
              | tail -n 1 \
              | cut -f2- \
              || true
            )"
          fi

          if [[ -z "$ZIP_FILE" || ! -f "$ZIP_FILE" ]]; then
            echo "::error::No Marzband source ZIP found."
            exit 1
          fi

          echo "Selected ZIP:"
          echo "$ZIP_FILE"

          unzip -q "$ZIP_FILE" -d "$GITHUB_WORKSPACE/extracted"

          ENGLISH_PUBSPEC="$(
            find "$GITHUB_WORKSPACE/extracted" \
              -type f \
              -path '*/apps/english/pubspec.yaml' \
            | head -n 1
          )"

          PERSIAN_PUBSPEC="$(
            find "$GITHUB_WORKSPACE/extracted" \
              -type f \
              -path '*/apps/persian/pubspec.yaml' \
            | head -n 1
          )"

          if [[ -z "$ENGLISH_PUBSPEC" || -z "$PERSIAN_PUBSPEC" ]]; then
            echo "::error::Both apps/english and apps/persian are required."

            find "$GITHUB_WORKSPACE/extracted" \
              -maxdepth 6 \
              -type f \
            | sort \
            | head -n 200

            exit 1
          fi

          PROJECT_ROOT="${ENGLISH_PUBSPEC%/apps/english/pubspec.yaml}"

          VERSION_RAW="$(
            awk '/^version:/ {print $2; exit}' "$ENGLISH_PUBSPEC"
          )"

          VERSION_NAME="${VERSION_RAW%%+*}"
          BUILD_NUMBER="${VERSION_RAW##*+}"

          if [[ -z "$VERSION_NAME" || -z "$BUILD_NUMBER" ]]; then
            echo "::error::Unable to read version from pubspec.yaml"
            exit 1
          fi

          echo "PROJECT_ROOT=$PROJECT_ROOT" >> "$GITHUB_ENV"
          echo "VERSION_NAME=$VERSION_NAME" >> "$GITHUB_ENV"
          echo "BUILD_NUMBER=$BUILD_NUMBER" >> "$GITHUB_ENV"

          {
            echo "ZIP=$ZIP_FILE"
            echo "ROOT=$PROJECT_ROOT"
            echo "VERSION=$VERSION_NAME"
            echo "BUILD=$BUILD_NUMBER"
          } | tee "$GITHUB_WORKSPACE/build-logs/source-info.log"

      - name: Generate Android projects and apply release overlays
        run: |
          set -Eeuo pipefail

          prepare_variant() {
            local language="$1"
            local app_dir="$PROJECT_ROOT/apps/$language"
            local log_dir="$GITHUB_WORKSPACE/build-logs"

            if [[ ! -f "$app_dir/pubspec.yaml" ]]; then
              echo "::error::Missing pubspec for $language"
              exit 1
            fi

            if [[ ! -d "$app_dir/tooling/android_overlay" ]]; then
              echo "::error::Missing Android overlay for $language"
              exit 1
            fi

            rm -rf "$app_dir/android"

            pushd "$app_dir" >/dev/null

            flutter create \
              --platforms=android \
              --project-name marzband \
              --org mk.kadkhodai \
              . \
              2>&1 \
            | tee "$log_dir/${language}-flutter-create.log"

            rsync -a \
              tooling/android_overlay/ \
              android/

            flutter pub get \
              2>&1 \
            | tee "$log_dir/${language}-pub-get.log"

            if [[ -f test/widget_test.dart ]] \
              && grep -qE '\bMyApp\s*\(' test/widget_test.dart
            then
              rm -f test/widget_test.dart
            fi

            popd >/dev/null
          }

          prepare_variant english
          prepare_variant persian

      - name: Validate English source
        run: |
          set -Eeuo pipefail

          cd "$PROJECT_ROOT/apps/english"

          flutter analyze \
            --no-fatal-warnings \
            --no-fatal-infos \
            2>&1 \
          | tee "$GITHUB_WORKSPACE/build-logs/english-analyze.log"

          if [[ -d test ]] \
            && find test \
              -type f \
              -name '*_test.dart' \
              -print \
              -quit \
            | grep -q .
          then
            flutter test \
              2>&1 \
            | tee "$GITHUB_WORKSPACE/build-logs/english-test.log"
          fi

      - name: Validate Persian source
        run: |
          set -Eeuo pipefail

          cd "$PROJECT_ROOT/apps/persian"

          flutter analyze \
            --no-fatal-warnings \
            --no-fatal-infos \
            2>&1 \
          | tee "$GITHUB_WORKSPACE/build-logs/persian-analyze.log"

          if [[ -d test ]] \
            && find test \
              -type f \
              -name '*_test.dart' \
              -print \
              -quit \
            | grep -q .
          then
            flutter test \
              2>&1 \
            | tee "$GITHUB_WORKSPACE/build-logs/persian-test.log"
          fi

      - name: Build English APK
        run: |
          set -Eeuo pipefail

          cd "$PROJECT_ROOT/apps/english"

          flutter build apk \
            --release \
            2>&1 \
          | tee "$GITHUB_WORKSPACE/build-logs/english-apk-build.log"

          cp \
            build/app/outputs/flutter-apk/app-release.apk \
            "$GITHUB_WORKSPACE/dist/Marzband-v${VERSION_NAME}-build${BUILD_NUMBER}-en.apk"

      - name: Build English AAB
        run: |
          set -Eeuo pipefail

          cd "$PROJECT_ROOT/apps/english"

          flutter build appbundle \
            --release \
            2>&1 \
          | tee "$GITHUB_WORKSPACE/build-logs/english-aab-build.log"

          cp \
            build/app/outputs/bundle/release/app-release.aab \
            "$GITHUB_WORKSPACE/dist/Marzband-v${VERSION_NAME}-build${BUILD_NUMBER}-en.aab"

      - name: Build Persian APK
        run: |
          set -Eeuo pipefail

          cd "$PROJECT_ROOT/apps/persian"

          flutter build apk \
            --release \
            2>&1 \
          | tee "$GITHUB_WORKSPACE/build-logs/persian-apk-build.log"

          cp \
            build/app/outputs/flutter-apk/app-release.apk \
            "$GITHUB_WORKSPACE/dist/Marzband-v${VERSION_NAME}-build${BUILD_NUMBER}-fa.apk"

      - name: Verify outputs
        run: |
          set -Eeuo pipefail

          cd "$GITHUB_WORKSPACE/dist"

          OUTPUT_FILES=(
            "Marzband-v${VERSION_NAME}-build${BUILD_NUMBER}-en.apk"
            "Marzband-v${VERSION_NAME}-build${BUILD_NUMBER}-en.aab"
            "Marzband-v${VERSION_NAME}-build${BUILD_NUMBER}-fa.apk"
          )

          for file in "${OUTPUT_FILES[@]}"; do
            if [[ ! -s "$file" ]]; then
              echo "::error::Output file is missing or empty: $file"
              exit 1
            fi

            sha256sum "$file" > "$file.sha256"
          done

          ls -lh

      - name: Upload English APK
        uses: actions/upload-artifact@v4
        with:
          name: marzband-en-apk-v${{ env.VERSION_NAME }}-build${{ env.BUILD_NUMBER }}
          path: |
            dist/*-en.apk
            dist/*-en.apk.sha256
          if-no-files-found: error
          compression-level: 0
          retention-days: 90

      - name: Upload English AAB
        uses: actions/upload-artifact@v4
        with:
          name: marzband-en-aab-v${{ env.VERSION_NAME }}-build${{ env.BUILD_NUMBER }}
          path: |
            dist/*-en.aab
            dist/*-en.aab.sha256
          if-no-files-found: error
          compression-level: 0
          retention-days: 90

      - name: Upload Persian APK
        uses: actions/upload-artifact@v4
        with:
          name: marzband-fa-apk-v${{ env.VERSION_NAME }}-build${{ env.BUILD_NUMBER }}
          path: |
            dist/*-fa.apk
            dist/*-fa.apk.sha256
          if-no-files-found: error
          compression-level: 0
          retention-days: 90

      - name: Collect failure diagnostics
        if: failure()
        run: |
          set +e

          mkdir -p "$GITHUB_WORKSPACE/build-logs"

          flutter doctor -v \
            > "$GITHUB_WORKSPACE/build-logs/flutter-doctor.log" \
            2>&1

          find "$GITHUB_WORKSPACE" \
            -maxdepth 7 \
            -type f \
          | sort \
            > "$GITHUB_WORKSPACE/build-logs/repository-files.log" \
            2>&1

      - name: Upload failure logs
        if: failure()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: marzband-failure-logs-${{ github.run_number }}
          path: build-logs/
          if-no-files-found: warn
          retention-days: 90
